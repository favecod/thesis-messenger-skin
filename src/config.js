const PORT = 2019
const websiteURL = `http://localhost:${PORT}`
const imageURL = `${websiteURL}/public/uploads/images`
const apiURL = `${websiteURL}/api/v1`

module.exports = {
    websiteURL,
    apiURL,
    imageURL,
}
